import { Modal } from "antd";

export type TodoItemType = {
  id: number;
  title: string;
  content: string;
  tag: string;
  status: string;
  createTime: string;
  lastModify: string;
  author: string;
};

export default function AddItem() {
  return <Modal>a</Modal>;
}
